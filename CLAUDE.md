## Project context

This is the DISA → OpenLDR v2 migration tool. Before making changes,
read `docs/PRD.md` in full. It defines the data model, transformation
rules, flag taxonomy, and non-goals.

Country-specific code mappings live in `config/*.yaml`. Never hardcode
codes — add them to the relevant YAML.
